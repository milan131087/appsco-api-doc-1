<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0">

        <meta name="description" content="">

        <title></title>

        <link rel="shortcut icon" href="favicon.png">

        {% block stylesheet %}
            <link type="text/css" rel="stylesheet" href="/vendor/bootstrap/dist/css/bootstrap.min.css" />
            <link rel="stylesheet" href="/vendor/highlight/src/styles/github.css">
            <link type="text/css" rel="stylesheet" href="/css/style.css" />
        {% endblock stylesheet %}
    </head>
    <body>
        <header class="container-fluid">
            <div class="row">
                <div class="col-md-2 col-md-offset-2 text-left">
                    <img src="https://appscomarket.com/img/footer-logo.png">
                </div>
                <div class="col-md-8 topmenu">
                    {% for item in topmenu %}
                        <a href="/{{ item.route }}">{{ item.name }}</a>
                    {% endfor %}
                </div>
            </div>
        </header>
        <section class="container-fluid">
            <div class="row">
                <div class="col-md-2 col-md-offset-2 sidemenu">
                    <ul class="nav nav-pills nav-stacked">
                    {% for item in sidemenu %}
                        <li {% if item.location == location %}class="active"{% endif %} >

                            <a href="/{{ item.route }}" >

                                {{ item.name }}
                                {% if item.method %}
                                    <span class="badge ">{{ item.method|upper }}</span>
                                {% endif %}
                            </a>
                        </li>
                        {% if item.children %}
                            <ul class="nav nav-pills nav-stacked">
                                {% for child in item.children %}
                                    <li {% if child.location == location %}class="active"{% endif %} ><a href="/{{ child.route }}" >{{ child.name }}</a></li>
                                {% endfor %}
                            </ul>
                        {% endif %}
                    {% endfor %}
                    </ul>
                </div>
                <div class="col-md-6 content">
                    {{ content|raw }}
                </div>
            </div>
        </section>

        <section class="container-fluid shade">
            <div class="row">
                <div class="col-md-8 col-md-offset-4 shaded"></div>
            </div>
        </section>

        <footer class="container-fluid">
            <div class="row">
                <div class="col-md-6 col-md-offset-2 text-left">
                    <p>Copyright 2014 © <a href="https://appsco.com">Appsco</a>, AS. All Rights Reserved.</p>
                </div>
                <div class="col-md-2 text-right ">
                    <img src="https://appscomarket.com/img/footer-logo.png">
                </div>
            </div>
        </footer>
        {% block javascript %}

            <script src="/vendor/highlight/build/highlight.pack.js"></script>
            <script>hljs.initHighlightingOnLoad();</script>

        {% endblock javascript %}
    </body>
</html>